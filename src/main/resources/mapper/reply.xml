<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.blog.model.ReplyRepository">
    <!-- <select id="findAllWithUser" resultType="shop.mtcoding.blog.dto.reply.ReplyResp$ReplyMainRespDto">
        select bt.id, bt.title, bt.thumbnail, ut.username
        from reply_tb bt
        inner join user_tb ut
        on bt.user_id = ut.id
    </select>

    <select id="findByIdWithUser" resultType="shop.mtcoding.blog.dto.reply.ReplyResp$ReplyDetailRespDto">
        select bt.id, bt.title, bt.content, bt.thumbnail, bt.user_id, ut.username
        from reply_tb bt
        inner join user_tb ut
        on bt.user_id = ut.id
        where bt.id = #{id}
    </select> -->

    <select id="findAll" resultType="shop.mtcoding.blog.model.Reply">
        select * from reply_tb
    </select>

    <select id="findById" resultType="shop.mtcoding.blog.model.Reply">
        select * from reply_tb where id = #{id}
    </select>    

    <insert id="insert">
        insert into reply_tb (comment, userId, boardId, created_at) values(#{comment}, #{userId}, #{boardId}, now())
    </insert>    

    <delete id="deleteById" >
        delete from reply_tb where id = #{id}
    </delete>    

    <update id="updateById" >
        update reply_tb set comment= #{comment} where id = #{id} 
    </update>
</mapper>